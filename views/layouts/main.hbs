<!DOCTYPE>  
<html>  
<head>  
  <title>Grand & Co</title>
  <style type="text/css">
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"; margin: 0 4em; }
    .box { box-sizing: border-box; }
    .header { border-bottom: 1px solid #c0c0c0; padding: .75em 0; margin-bottom: 1.5em; }
    .main { width: 75%; float: left; }
    .sidebar { width: 25%; float: right; padding-left: 1.5em; }
    .small { font-size: 0.75rem; }
    .text-right { text-align: right; }
    .wrapper {
      display: inline-block;
    }
    .parent {
      display: flex;
      flex-direction: column;
    }
    .child {
      border: none;
    }
    .delete-btn { padding-right:3px; }
    .delete-btn:hover { cursor: pointer; }
  </style>
    
  {{{block "head"}}}
    
  </script>

  
</head>  
  <div class="header">
    <h1 id="header-title" style="margin-bottom:0.25rem;">Grand &amp; Co</h1>
    <small><a href="" id="pipedrive-link"></a></small>
    </p>
  </div>
  <div class="box main">
    {{{body}}}
  </div>
  <div class="box sidebar" id="sidebar">
    {{{ block "sidebar" }}}
  </div>
</html>  